{% layout none %}{
    "items_subtotal_price": {{cart.items_subtotal_price | json}},
    "total_price": {{cart.total_price | json}},
    "item_count": {{cart.item_count | json}},
    "items": [{% for item in cart.items %}{"item":{{item |json}}, "variant":{{item.variant | json}}, "product":{{item.product | json}}}{% unless forloop.last %},{% endunless %}{%- endfor -%}]
}
